%h1 Projects
.row
  .col-sm-12
    - @projects.each do |project|
      %project_card{data: {type: 'projects', project_id: project.id}}
.row
  .col-sm-12
    = link_to 'Back', dashboard_path, class: 'btn btn-info btn-sm'
    = link_to 'New', new_project_path, class: 'btn btn-info btn-sm'
